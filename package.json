{
  "name": "web-job-finder",
  "version": "1.0.0",
  "description": "Web Job Finder — Negocios cerca y lista de posibles clientes",
  "private": true,
  "scripts": {
    "config": "node scripts/inject-env.js",
    "dev": "node server.js",
    "start": "node server.js"
  }
}
